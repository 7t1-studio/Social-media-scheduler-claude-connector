# Connect ChatGPT to So-me Studio MCP

**Status: coming soon**

So-me Studio will ship as a **ChatGPT plugin / MCP app** after OAuth and marketplace review are complete. The hosted endpoint will remain:

`https://api.so-me.studio/mcp`

## What to expect

1. Install **So-me Studio** from the ChatGPT plugin / apps directory (or enable it for your workspace when available).
2. Complete **OAuth** to link your so-me.studio account (Team+ plan for API / posting entitlement).
3. Use natural-language prompts to list posts, schedule content, check inbox, or pull analytics — without pasting an API key.

## Until then

- Use **Claude** ([setup-claude.md](setup-claude.md)) or **Cursor** ([../config/cursor.mcp.json.example](../config/cursor.mcp.json.example)) with `X-API-Key`.
- Product MCP docs: https://docs.so-me.studio/mcp/overview
- Website: https://so-me.studio

This page will be updated with exact ChatGPT connect steps after OpenAI listing approval.
